apiVersion: v1
kind: Service
metadata:
    name: {{ .Chart.Name }}
    labels:
        suite: fyodorov
        service: gagarin
spec:
    type: {{ .Values.service.type }}
    ports:
      - port: 8000
        targetPort: 8000
        protocol: TCP
        name: gagarin
    selector:
      app.kubernetes.io/name: {{ .Chart.Name }}
      app.kubernetes.io/instance: {{ .Release.Name }}